# Web Studio — 遂行ロジック

`web-studio` スキルの実行フロー。手法の本文は [SKILL.md](SKILL.md)。
要は3本柱:**キットだけが直列スパイン / 重い待ちは承認待ちの裏に隠す / 契約が一貫性を運ぶ**。

## ① 実行フロー

```mermaid
flowchart TD
  GO(["依頼"]) --> CD{"クールダウン環境?"}
  CD -- "あり" --> FIX["回避を先に織り込む<br/>ピン下げ・手動配線・min-release-age=0"]
  CD -- "なし" --> FORK
  FIX --> FORK
  FORK{{"同時起動 ⑂ ここから2レーンが並走（あなたは待たない）"}}

  FORK --> P1
  FORK --> L0

  subgraph DESIGN ["クリティカルパス＝あなたが書く"]
    P1["フェーズ1 設計<br/>PLAN.md ＋ 画像マニフェスト"]
    P1 --> APPROVE{{"承認ゲート — 停止"}}
  end

  subgraph BG ["背景レーン（承認待ち＝死に時間に隠す・あなたは待たない）"]
    direction TB
    L0["レーン0 足場<br/>scaffold ＋ 全lib install"]
    LB["レーンB 画像<br/>unsplash-fetch → /public/img"]
  end

  P1 -. "マニフェスト確定で分岐" .-> LB
  APPROVE -- "承認" --> P2
  L0 -. "install完了で合流" .-> P2

  P2["フェーズ2 トークン ＋ 型付きキット<br/>直列スパイン・最小契約で凍結"] --> CON[["キット契約を発行<br/>Props署名・トークン・画像スロット<br/>＝唯一の真実／最初に読む索引"]]
  CON --> FO{{"ファンアウト（凍結後のみ）"}}

  subgraph P3 ["フェーズ3 ページ合成（並列・1ページ1エージェント）"]
    direction LR
    PA["index.astro"]
    PB["about.astro"]
    PC["… page N"]
  end
  FO -- "契約 ＋ 担当PLAN ＋ 役割宣言" --> PA & PB & PC
  LB -. "同パスへ実画像を後差し（無編集スワップ）" .-> P3

  PA & PB & PC --> CHECK{{"astro check 緑?<br/>型＝契約・fail-fast"}}
  CHECK -- "型エラー" --> FIXT["契約違反を修正"]
  FIXT --> CHECK
  CHECK -- "緑" --> BUILD

  subgraph LOOP ["フェーズ4 視覚自己批評ループ（本体）"]
    BUILD["build → dist 配信"] --> SHOT["撮影モードで reveal可視化<br/>1440 ＆ 375 で全画面スクショ"]
    SHOT --> CRIT["マルチモーダル批評（各指摘を 局所／系統 に分類）"]
    CRIT --> STOP{"停止条件 全Yes?<br/>階層・余白・可読性・375・一貫性・品位"}
    STOP -- "No・局所" --> PATCH["per-page ピンポイント修正"] --> BUILD
  end

  STOP -. "No・系統＝共有部品（blast radius≧2）<br/>キットを1回修正→契約再発行→該当ページ再撮影" .-> P2
  STOP -- "Yes" --> P5["フェーズ5 ゲート段<br/>astro check → build →（任意 a11y／perf／視覚）"]
  P5 --> OUT(["dist/ ＝ 純静的・CDN可"])
```

## ② 並列タイムライン（核心＝死に時間に隠す）

```
クリティカルパスはこれだけ →   設計 ▶ キット ▶ ページ ▶ 自己批評

                       ┌─【あなたの手】設計 ＋ 画像マニフェスト ──[承認で停止]──┐
依頼 ─(クールダウン検出)─◆同時起動                                            ├─▶ キット凍結 ▶ ⟦契約発行⟧
                       └─【裏で並走・BG】レーン0 install ▓▓▓▓▓▓▓ ───────────┘   ← 承認待ち＝死に時間に完走（後段の停止ゼロ）
                            （レーンB 画像 ▓▓▓▓ はマニフェスト確定後に分岐／パス＝契約で placeholder へ無編集スワップ）

  ▶ ページ並列（1p1agent・契約を手渡し）▶ astro check【型ゲート/fail-fast】▶ ループ⟳（build→撮影→批評→局所修正 ／ 系統欠陥はキットへ戻す ／ 全Yesまで）▶ build ▶ dist/
```

## 読み方（3本柱）

1. **唯一の直列スパイン＝キット**。最小契約で速く凍結したら、ページは即ファンアウト（並列）。
2. **重い待ち（install・画像API）は承認待ちの裏**。クリティカルパスに乗せない＝体感速度が出る。
3. **契約が一貫性を運ぶ**。
   - **キット契約**(Props署名・トークン・画像スロット＝唯一の真実)を各ページエージェントへ手渡し → 別コンテキストでも把握して役割判断、推測ゼロ。
   - **画像はパスが契約** → ビルダーは placeholder で即ビルド、画像レーンが同じパスへ後差し（`aspect-[…]` 固定でシフトしない）。

**品質の閉ループ＝フェーズ4**(自分の出力を見て直す)、**最終保証＝フェーズ5ゲート**(型→ビルド→任意でa11y/perf/視覚)。`astro check` は**ファンアウト直後にも緑化**(型＝契約の fail-fast・`astro build` は型を見ない)＝視覚ループに型崩れを持ち込まない。批評で出た**系統欠陥(共有部品)はくびれ＝キットに戻して1回直す**(per-page で各自パッチすると一貫性が割れる)。
